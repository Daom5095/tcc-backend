# TCC - Plataforma Colaborativa (Backend)

## Resumen
Backend del proyecto: Node.js + Express + Socket.io + MongoDB.

## Cómo ejecutar (local)
1. Copia `.env.example` a `.env` y completa variables.
2. `npm install`
3. `npm run dev` (usa nodemon)

## Estructura sugerida
src/
  app.js
  server.js
  modules/
  models/
  routes/
  sockets/
  utils/
